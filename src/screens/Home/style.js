import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "white",
    color: "red",
    padding: 20,
    flex: 1,
    gap: 30,
  },
  card: {
    flexDirection: "row",
    gap: 10,
    width: "80%",
  },
});
